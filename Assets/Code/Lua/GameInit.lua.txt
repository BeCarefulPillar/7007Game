--GameInit.Lua 脚本、初始化所有的View脚本

 -- CtrlMgr.lua  管理控制器脚本、里面table 表存储其名字及其对应的ctrl脚本的实例 --
local M = {};

--初始化方法 view 同时加载
function M:Init()
    --这里的方法就是负责把UI给克隆出来  遇到方法时 使用 : 加上后面的方法
    VIEW_MGR:LoadView("ViewPrefab/UIRootView")
    print("111111111111")
    --self:loadView(ViewNames.UIRootView);
end

return M